<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- This disables the black preview screen FLAG_ACTIVITY_CLEAR_TASK-->
        <item name="android:windowDisablePreview">true</item>
    </style>
    <style name="MyToggleButton" parent="@android:style/Widget.CompoundButton">
        <item name="android:button">@drawable/select_toggle_btn</item>
        <item name="android:gravity" >center</item>
    </style>

    <style name="hintDialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <!-- 弹出框动画 顶部下拉和上提 -->
    <style name="virtualKeyboardDialogAnim" parent="@android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@anim/slide_in_top</item>
        <item name="android:windowExitAnimation">@anim/slide_out_top</item>
    </style>

    <style name="AnimDown" parent="@android:style/Animation">
        <item name="android:windowEnterAnimation">@anim/down_toward_scale_in</item>
        <item name="android:windowExitAnimation">@anim/down_toward_scale_out</item>
    </style>
    <style name="AnimHorizontal" parent="@android:style/Animation">
        <item name="android:windowEnterAnimation">@anim/push_scale_left_in</item>
        <item name="android:windowExitAnimation">@anim/push_scale_left_out</item>
    </style>
    <style name="AnimRight" parent="@android:style/Animation">
        <item name="android:windowEnterAnimation">@anim/push_scale_right_in</item>
        <item name="android:windowExitAnimation">@anim/push_scale_right_out</item>
    </style>
    <style name="AnimUp" parent="@android:style/Animation">
        <item name="android:windowEnterAnimation">@anim/push_bottom_in</item>
        <item name="android:windowExitAnimation">@anim/push_bottom_out</item>
    </style>
    <style name="AnimUp1" parent="@android:style/Animation">
        <item name="android:windowEnterAnimation">@anim/up_toward_scale_in</item>
        <item name="android:windowExitAnimation">@anim/up_toward_scale_out</item>
    </style>
</resources>
